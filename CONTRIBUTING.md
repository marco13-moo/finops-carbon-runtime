# Contributing

1. Create a focused branch and add tests for behavior changes.
2. Run `python -m unittest discover -s tests -v` and `python -m finops_runtime benchmark`.
3. Keep accounting formulas and assumptions documented in `docs/adr/`.

Pull requests should explain user impact, validation, and any changes to allocation or policy semantics.
