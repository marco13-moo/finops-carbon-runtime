[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "finops-carbon-runtime"
version = "0.1.0"
description = "Platform FinOps and carbon-aware runtime MVP"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.mypy]
strict = true
